import { Container, Text } from '@mantine/core';

const SchoolListPage = (): JSX.Element => {
  return (
    <Container>
      <Text c="dimmed">Schools will be listed here</Text>
    </Container>
  );
}

export default SchoolListPage;
